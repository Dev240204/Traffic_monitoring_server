const express = require('express');
const mongoose = require('mongoose');
const Roads = require('../models/roads');

const roadRouter = express.Router();

// roadRouter.post("/add",(req,res)=>{
//     const data = req.body;
//     const road =
// })

module.exports = roadRouter;